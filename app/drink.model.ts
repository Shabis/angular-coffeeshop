export class Drink {
  constructor(public name: string, public size: string, public shots: number, public flavor: string, public milk: string, public type: string, public company: string, public price: number) {}
}
